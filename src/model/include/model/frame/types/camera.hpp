#pragma once

namespace edgevision::model::frame {

    struct CameraIntrinsics {
        float fx = 0.0f;
        float fy = 0.0f;
        float cx = 0.0f;
        float cy = 0.0f;
    };

} // namespace edgevision::model::frame
